{
  "name": "Telegram Shipping Bot",
  "description": "WhiteLabel Telegram Shipping Bot with ShipStation Integration",
  "repository": "https://github.com/emergent/telegram-shipping-bot",
  "keywords": ["telegram", "bot", "shipping", "shipstation"],
  "env": {
    "MONGO_URL": {
      "description": "MongoDB Atlas connection string",
      "required": true
    },
    "DB_NAME": {
      "description": "MongoDB database name",
      "required": true
    },
    "TELEGRAM_BOT_TOKEN": {
      "description": "Telegram Bot API Token",
      "required": true
    },
    "ADMIN_TELEGRAM_ID": {
      "description": "Admin Telegram User ID",
      "required": true
    },
    "ADMIN_API_KEY": {
      "description": "Admin API Key for frontend access",
      "required": true
    },
    "BOT_MODE": {
      "description": "Bot mode: polling or webhook",
      "value": "polling",
      "required": false
    },
    "BOT_ENVIRONMENT": {
      "description": "Bot environment: production or test",
      "value": "production",
      "required": false
    }
  },
  "buildpacks": [],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "addons": [],
  "scripts": {
    "postdeploy": "echo 'Skipping MongoDB migration - using external MongoDB Atlas'"
  }
}
